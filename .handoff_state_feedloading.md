# AGENT STATE & HANDOFF

## 1. Meta-Informationen
- **Gesamtaufgabe:** FEEDS.md — Wechsel des Feed-Ladevorgangs von URL-basiert (`POST /api/feed/fetch`) auf DB-ID-basiert (`GET /api/feed/:id`). Phasen 1–6 (Metadaten-Persistierung, Leseendpunkt, Frontend-Umstellung, Preview-Frage, YouTube/Randbedingungen, Migration+Verifikation).
- **Status:** READY_FOR_HANDOFF
- **Letzter Aktualisierungszeitpunkt:** Phase-6-Run 1

## 2. Abgeschlossene Schritte (Done)
- [x] Phase 1 (alle Schritte 1.1–1.5): `downloads` um timestamp/pub_date/duration/description/content/is_youtube erweitert; register/enqueue leidet Metadaten; Parser liefert alle Felder bereits. ALTER TABLE bereitgestellt.
- [x] Schritt 2.1 (`server/services/feedService.js`): `getFeedById(userId, feedId)` — Subscription-Read (Owner-GATE, episodes_count-Subquery) + `downloads JOIN subscriptions` (podcastTitle=title, feedUrl=feed_url), Order BY timestamp DESC, created_at DESC. Output `{ feed, episodes }`; kein Subscription → `{ feed: null, episodes: [] }`.
- [x] Schritt 2.2 (`server/routes/FeedRoutes.js`): `GET /:id` mit `requireAuth`; validiert non-empty id; ruft getFeedById; 200 mit Result. POST /fetch unverändert.
- [x] Schritt 2.2b (`server/routes/index.js`): `/feed`-Router bekommt requireAuth + Modelle weitergegeben.
- [x] Schritt 2.3 (`server/routes/routes.test.js`): 3 neue Tests (auth 401, Owner-Shape, User-Scoping/404). feedService.test.js 4/4.
- [x] **Phase 3 (Frontend):**
  - [x] Schritt 3.1 (`public/js/api.js`): `loadFeedById(feedId)` → `GET /api/feed/:id` → `{ feed, episodes }`. `addSubscription` liefert nun Body. `fetchFeed`/`fetchFeeds` blieben bisher (Preview, Phase 4).
  - [x] Schritt 3.2 (`public/js/state.js`): `feedIdByUrl = {}` in Constructor + Reset.
  - [x] Schritt 3.2 (`public/js/sync.js`): `syncFeedsWithServer()` baut `state.feedIdByUrl[url]=id`; `saveFeedToServer()` aktualisiert Map; Map bei Add frisch.
  - [x] Schritt 3.3 (`public/js/feeds.js`): `fetchSingleFeed(url,…)` löst `url→id` auf → `loadFeedById(id)`; Fallback auf `fetchFeed(url)`. Normalisiert beide Response-Shapes. `removeFeed()` löscht `feedIdByUrl[url]`.
- [x] **Phase 4 (Preview-Frage §6.1 — Option a umgesetzt):**
  - [x] Entscheidung dokumentiert: Preview **beibehalten, streng auf nicht-subskribierte URLs beschränkt** (einziger verbleibender URL-Fetch). §6.1 + §8 Schritt 4.1 in FEEDS.md aktualisiert.
  - [x] `public/js/api.js`: tote Bulk-Methode `fetchFeeds(urls)` entfernt; nur `fetchFeed(url)` bleibt (Kommentar auf Preview-only präzisiert).
  - [x] `public/js/feeds.js`: Preview-Bedingung `totalCount === 0 && !isSubbed` mit Kommentar zur Einordnung (einzig verbleibender URL-Fetch, nicht-subskribiert) versehen; Fallback in `fetchSingleFeed` auf `fetchFeed` als transientes Add-Race-Sicherheitsnetz dokumentiert.

- [x] **Phase 5 (YouTube & Randbedingungen):**
   - [x] Schritt 5.1 (`downloads.playlist_id VARCHAR(64)`): schema.sql `CREATE TABLE` + idempotentes `ALTER TABLE ... ADD COLUMN IF NOT EXISTS playlist_id` (Migrator wendet schema.sql jedes Mal an). `Downloads.js`: `playlist_id` in `UPDATABLE` + `create()`-INSERT. `downloadsService.register()`: nimmt `playlistId` an → `create`. `SubscriptionRoutes.enqueueEpisodesForFeed`: `playlistId: episode.playlistId || null` (neben `isYoutube`).
   - [x] Schritt 5.1b (`youtubeService.js fetchFeed`): spritzt `playlistId` bei Atom/Videos.xml-Pfade in jede Episode (`isYouTube` bereits gesetzt); OEmbed-Single hat `playlistId`/`isYouTubePlaylist` bereits.
   - [x] Schritt 5.1c (`feedService.getFeedById`): liefert `e.playlist_id AS playlistId` + rekonstruiert `isYouTubePlaylist` (`CASE WHEN playlist_id IS NOT NULL AND <> ''`) → YouTube-Playlist-Playback aus DB (`playback.js`) funktioniert.
   - [x] Schritt 5.2 (Offline-Audio): unverändert bestätigt — SW-Cache `podany-audio-v1` (config.js:39), `audioProxyUrl`/`streamAudio` (api.js:224/228), Cache-Invalidation (modal.js:292); keine Frontend-Audio-Änderung.
   - [x] Test: `feedService.test.js` +2 (playlistId/isYouTubePlaylist-Mapping, Empty-Shape). Gesamtdurchlauf feedService.test.js 6/6; routes.test.js 21/28 (7 Fehler pre-existing, Phase-5-Baseline: 19 pass → Phase 5 fixt 2, bricht 0).

- [x] **Phase 6 (Migration Bestandsdaten & Verifikation) ABGESCHLOSSEN:**
  - [x] Schritt 6.1 (`server/db/migrator.js`): `splitStatements()`-Bug behoben — Full-Line/Inline-Comments (`-- …`) wurden bisher nichtstripped (Text blieb, an next.Statement gehängt → letztes schema.sql-Statement war ungültiges Kommentar+ALTER-Gemisch, DB-migrate gefailed). Jetzt `line.slice(0, dashIndex)`. `server/db/schema.sql`: 7 idempotente `ALTER TABLE downloads ADD COLUMN IF NOT EXISTS …`-Guard für timestamp/pub_date/duration/description/content/is_youtube/playlist_id ergänzt (bisher nur playlist_id; die 6 Metadaten standen nur in CREATE TABLE = frische DBs). `npm run migrate` legt bestehende DBs fehlerfrei + wiederholungssicher an. Regression: `server/db/migrator.test.js` (+3, alle grün).
  - [x] Schritt 6.2 (Frontend statisch verifiziert, Sandbox ohne Browser): DB-ID-Laden (`feeds.js:111`→`api.js:190`), url→id-Mapping (`sync.js:27/50`), Preview nur nicht-subskribiert (`feeds.js:737`: `totalCount===0 && !isSubbed`), Add-Race-Fallback `fetchFeed` nur ohne `id` — alles korrekt. Bug: `timeline.js` newest/älteste/podcast-Sortierung roh `b.timestamp-a.timestamp` → NaN bei Bestandsdaten; jetzt null-safe `(x.timestamp||0)` (deckungsgleich mit Continue-Guard `timeline.js:91,155`).
  - [x] Schritt 6.3: esbuild-Build exit 0 (`public/dist/bundle.js`, 122.1 kb). Kein Lint/Typecheck-Skript vorhanden. `node --test`: 123 Tests / 116 pass / **7 fail pre-existing** (auth+sync Routes 404 statt 200) — gegen HEAD-Baseline identischer Failure-Set, +3 neue Tests, 0 neue Fehler.

## 3. Aktueller Haltepunkt (Last Position)
- **Zuletzt bearbeitete Entität:** Phasen 1–6 vollständig. Phase 6: `server/db/migrator.js` (Comment-Stripping-Bug), `server/db/schema.sql` (7 ALTER-Guards), `public/js/timeline.js` (null-safe Sortierung), `server/db/migrator.test.js` (neu), `public/FEEDS.md` + `.handoff_state.md` aktualisiert, `public/dist/bundle.js` neu gebaut.
- **Zustand des Codes/Dokuments:** Alle 6 Phasen implementiert und verifiziert. Server-Leseendpunkt `GET /api/feed/:id` (DB-only, kein URL-Fetch/Parse bei Subskriptionen). Bestandsdaten-Migration idempotent (`npm run migrate` legt alle neuen `downloads`-Spalten an). Frontend sortiert null-sicher. Build grün. Offene Restmenge: 7 pre-existing Test-Fehler in auth/sync-Routes (unabhängig von FEEDS.md, nicht im Scope).

## 4. Sofortige nächste Aktionen (Next Steps)
1. **Keine aktiven Phase-6-Aufgaben mehr.** Phasen 1–6 sind abgeschlossen und dokumentiert (`READY_FOR_HANDOFF`).
2. **Optional/nicht im FEEDS-Scope:** Die 7 pre-existing `auth`/`sync`-Route-Fehler (404 statt 200) separat untersuchen — entstehen vermutlich aus fehlender DB-/Auth-Integration im Test-Sandbox-Setup, nicht aus diesem Change.
3. **Produktion:** `npm run migrate` auf Bestandsdaten ausführen, damit alle neuen `downloads`-Spalten gefüllt werden; danach Frontend-Manuell-Check (Refresh ohne Netz) in echter Umgebung.

## 5. Kritischer Kontext & Variablen (Working Memory)
- **Episode-Shape (Client):** `guid,title,podcastTitle,feedUrl,timestamp,pubDate,duration,artwork,description,content,isYouTube,audioUrl`. Server-Join liefert `podcastTitle`/`feedUrl`; Parser (`podcastService.js`) setzt `feedUrl=originalUrl` bei URL-Fetch. Merge in `refreshAllFeeds()` filtert über `state.feeds.includes(ep.feedUrl)` → funktioniert für beide Pfade.
- **Preview-Regel (Phase 4):** URL-Fetch nur wenn `!isSubbed && totalCount === 0` (`renderFeedDetail`, feeds.js:738). Subskribiert → immer DB-Lesezugriff. Fallback `fetchFeed` in `fetchSingleFeed` (feeds.js:109) nur ohne bekannte `id` (Add-Race, `saveFeedToServer` hat id noch nicht zurückgegeben).
- **DB:** MariaDB via mysql2. Migrator wendet schema.sql jedes Mal komplett an (CREATE TABLE IF NOT EXISTS) → fügt bestehenden Tabellen KEINE Spalten hinzu; manuelles ALTER TABLE für Bestandsdaten nötig (Phase 1 bereitgestellt). Phase 5 fügt idempotentes `ALTER TABLE downloads ADD COLUMN IF NOT EXISTS playlist_id VARCHAR(64)` in schema.sql ein (MariaDB unterstützt ADD COLUMN IF NOT EXISTS) → bleibt bei wiederholter Migration sicher, Bestandsdaten bekommen die Spalte beim nächsten `npm run migrate`.
- **YouTube-Metadaten (Phase 5):** `downloads.playlist_id VARCHAR(64)` persistiert `playlistId`; `is_youtube` persistiert `isYouTube`. `getFeedById` liefert `playlistId` + rekonstruiert `isYouTubePlaylist` (CASE auf non-empty playlist_id) → deckungsgleich mit Fetch-Shape, YouTube-Playlist-Playback (`playback.js:345,383`) auch aus DB. Atom-Pfad (`youtubeService.js`) spritzt `playlistId` nach; OEmbed-Single bereits gesetzt.
- **Auth-Muster:** `requireAuth` aus `middleware/auth.js`; Token über `x-session-token` Header oder Session-Cookie. Neue Endpunkte auth-pflichtig.
- **Add-Race:** `POST /api/subscription` erstellt Subscription-Row (awaited) vor Response, Enqueue der Episoden läuft asynchron (`void`). Erster `loadFeedById` nach Add kann daher kurz leer sein; healt sich beim nächsten Refresh aus DB. Map wird via `saveFeedToServer`-Return-id sofort gesetzt.
- **Build/Verifikation:** Frontend-Build über `/home/mk/projects/podany/node_modules/.bin/esbuild public/js/main.js --bundle --minify --outfile=... --format=esm` (native binary, NICHT via node ausführen). Server-Tests: `node --test` (123/116 pass, 7 pre-existing auth/sync failures). Kein Lint/Typecheck-Skript im Projekt.
- **Migrator-Bug (Phase 6.1, behoben):** `splitStatements()` schritt bisher nur das `--`-Präfix ab, nicht den Comment-Text → Full-Line-Comments hingen am next.Statement und machten das letzte schema.sql-Statement ungültig. Fix: `line.slice(0, dashIndex)`. Schema hat keine `--` in String-Literalen, daher sicher. `migrator.test.js` deckt es ab.
- **Bestandsdaten-Migration (Phase 6.1):** `schema.sql` enthält jetzt 7 idempotente `ALTER TABLE downloads ADD COLUMN IF NOT EXISTS …`-Guard für timestamp/pub_date/duration/description/content/is_youtube/playlist_id. Fresh DBs: via CREATE TABLE; Bestands: via ALTER beim nächsten `npm run migrate`. Backfill nicht zwingend (Add/Refresh füllt neu).
- **Sortierung (Phase 6.2):** `timeline.js` newest/älteste/podcast rechnete roh `b.timestamp-a.timestamp` (NaN bei fehlendem timestamp); jetzt `(x.timestamp||0)`, deckungsgleich with Continue-Guard (`timeline.js:91,155`).
