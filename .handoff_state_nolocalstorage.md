# AGENT STATE & HANDOFF

## 1. Meta-Informationen
- **Gesamtaufgabe:** localStorage im Frontend entkoppeln; Server als primäre Datenquelle (Source of Truth). Umsetzung der Schritte aus `public/LOCALSTORAGE.md`.
- **Status:** READY_FOR_HANDOFF (Schritte 0–8 + Build-Validierung abgeschlossen; Schritt 9 optional offen)
- **Letzter Aktualisierungszeitpunkt:** Build-Rebuild — 2026-09-26T06:05Z

## 2. Abgeschlossene Schritte (Done)
- [x] Schritt 0 — Entscheidungen fixieren: Klassifizierung aller 8 `STORAGE_KEYS` gegen Code verifiziert; THEME/QUEUE entschieden (beide client-only, transient/Speicher); Server-Seite bestätigt (subscriptions, playback_state, downloads Models + Routes; SESSION über Cookie + X-Session-Token). Ergebnis in `public/LOCALSTORAGE.md` Abschnitt 2 („Entscheidungen") und Abschnitt 6 (Schritt 0) dokumentiert; 2 Risiken in Abschnitt 7 ergänzt.
- [x] Schritt 1 — Server-Lücken schließen: kein Server-Change, da THEME/QUEUE client-only. Persistenten Quellen (subscriptions/playback_state/downloads/SESSION) bereits abgedeckt bestätigt. In `public/LOCALSTORAGE.md` Abschnitt 6 (Schritt 1) dokumentiert.
- [x] Schritt 2 — API-Erweiterungen in `public/js/api.js`: FEEDS/POSITIONS bereits vorhanden+korrekt bestätigt; DOWNLOADS ergänzt (`listDownloads` GET `/api/downloads`, `registerDownload` POST, `removeDownload` DELETE) — Endpunkte matchen `DownloadRoutes.js`; optionales `loadAll()` für den Boot ergänzt. Manuell verifiziert (esbuild-Build in Sandbox nicht ausführbar). In `public/LOCALSTORAGE.md` Abschnitt 6 (Schritt 2) dokumentiert.
- [x] Schritt 3 — `public/js/storage.js` als API-Adapter neu implementiert (Konstruktor `Storage(config, api, state)`). localStorage vollständig entfernt (kein getItem/setItem/clear/Legacy/Migration). Server-backed async: loadFeeds/loadPositions/loadDownloads + saveDownloads(server). saveFeeds/savePositions = No-op (Server-Schreibweg über sync-manager/api.savePosition). THEME/QUEUE/CACHE synchron im Speicher → theme.js/queue.js/feeds.js unverändert. Session nur interners state-Token. Aufrufer angepasst: main.js (Storage-Ktor, loadPersistedState async, init async awaiting loadPersistedState zur Reihenwahlerhaltung checkUrlSessionParam→checkAuth); downloads.js (loadDownloads async). clearAll cleared nur client-transiente Zustände. In `public/LOCALSTORAGE.md` Abschnitt 6 (Schritt 3) dokumentiert.
- [x] Schritt 4 — Session/Auth ohne localStorage: verifiziert + dokumentiert. Token lebt nur in `state.sessionToken`, kein Browser-Speicher. Cookie `podcast_session` über `credentials:'include'` (`api.js:31`), Name matcht Server (`defaults.js:19`); Logout löscht Cookie (`auth.js:263`). `X-Session-Token`-Header aus State (`api.js:20-25`, Logout explizit `api.js:87`). `storage.js:20-28` load/saveSessionToken nur interner State. Kein Session-`localStorage`-Schreibzugriff anywhere. Verbleibende localStorage-Refs: `podany_player_collapsed` (playback.js, UI) + `localStorage.clear()` (ui/modal.js:275 resetAll → Schritt 8/Risiko). In `public/LOCALSTORAGE.md` Abschnitt 6 (Schritt 4) dokumentiert.
- [x] Schritt 5 — Boot asynchron / auth-first in `public/js/main.js`: `loadPersistedState()` durch `async initApp()` ersetzt (`main.js:49-81`). Reihenfolge: checkUrlSessionParam() → checkAuth() → loadFeeds/loadPositions/loadDownloads → loadCache+loadQueue → UI-Render. `init()` awaitet initApp(); trailing checkAuth() entfallen. Ladezustand über modal.showStatus/hideStatus (status-banner) während Server-Calls. Debug-console.log entfernt. checkUrlSessionParam läuft jetzt vor checkAuth (Magic-Link-korrekt); checkAuth vor Datenladen = auth-first. Event-Wiring bleibt nach initApp (checkAuth braucht keine Handler). Redundanz bei Login: checkAuth syncnt Feeds/Positionen, loadFeeds/loadPositions laden erneut (funktionally sicher, geräteübergreifend korrekt; optional via api.loadAll reduzierbar). Kein Rest-Zeichen von loadPersistedState im Code (nur Doku). In `public/LOCALSTORAGE.md` Abschnitt 6 (Schritt 5) dokumentiert.
- [x] Schritt 6 — Cache-Strategie: verifiziert + dokumentiert. CACHED_EPISODES/CACHED_METADATA NICHT in localStorage. Cache im Arbeitsspeicher: state.js (allEpisodes=[]/feedMetadata={}), storage.js loadCache/saveCache nur state.allEpisodes/feedMetadata, Boot main.js:67-69 über loadCache(), feeds.js refreshAllFeeds zieht vom Feed nach + saveCache. Offline-Audio unverändert über SW Cache API (AUDIO_CACHE_NAME='podany-audio-v1'): sw.js:2,26,62 + downloads.js + feeds.js (caches.open/delete). grep localStorage public/js (.js): nur podany_player_collapsed (playback.js:452,767) + localStorage.clear() (ui/modal.js:275 resetAll → Schritt 8). Stales Comment downloads.js:2 korrigiert ("metadata in localStorage" → in-memory AppState cache). STORAGE_KEYS.CACHED_EPISODES/METADATA (config.js:8-9) jetzt tote Konstanten, nicht gelöscht (Scope). In `public/LOCALSTORAGE.md` Abschnitt 6 (Schritt 6) dokumentiert.
- [x] Schritt 7 — Offline-/Fehlerverhalten: try/catch-Slurping aus storage.js Load-Methoden entfernt (loadFeeds/loadPositions/loadDownloads, storage.js:32-85) → Fehler propagieren. main.js: handleBootError() (main.js:48-59) + try/catch um initApp Schritt 3 (main.js:80-87); ApiError import (main.js:14). 401/403 → sessionToken leeren + showAuthModal + 'Session Expired'; sonst console.error + modal.showStatus im status-banner; return vor UI-Render (kein stilles leeres Grid). sync.js (api.* direkt, eigene 401/403-Behandlung) unaffected. Alle 3 Load-Methoden nur im Boot aufgerufen → Slurping-Entfernung safe. offline-badge (setupNetworkListeners) zeigt separat Offline. In `public/LOCALSTORAGE.md` Abschnitt 6 (Schritt 7) dokumentiert.
- [x] Schritt 8 — localStorage-Zugriffe vollständig entfernt + Legacy-Migration raus: (a) `podany_player_collapsed` (playback.js:452,767) → client-only transient in AppState: `state.playerCollapsed` (state.js:28,60); playback.js liest/schreibt nur noch state. (b) `localStorage.clear()` in `ui/modal.js:274 resetAll()` → durch server-seitige Löchaufrufe ersetzt: resetAll() jetzt async, löscht Feeds über `api.removeSubscription`, Positionen über neue `api.removePosition` (DELETE `/api/playback/positions`), Downloads über `api.removeDownload`; SW-Cache `caches.delete('podany-audio-v1')` bleibt; danach `state.reset()`. (c) config.js: `LEGACY_KEYS`-Block + `Config.legacyKey()` entfernt. Ziel erreicht: 0× localStorage-Zugriff in public/js/** (nur noch Kommentar storage.js:5). Server: PlaybackRoutes.js DELETE `/positions` + playbackService.removePosition(userId, guid) (Model.remove existierte bereits). Validierung: `node --test server/routes/routes.test.js` = 18 pass / 7 fail (identisch zu Baseline — 7 Failures sind PRE-EXISTING, Test nutzt alte `/api/sync/*`-Pfade vs. Source `/api/playback/*`); playbackService.test.js 3/0. **Kein Regression durch Schritt 8.** Build (`npm run build`, esbuild) in Sandbox nicht ausführbar → in produktiver Umgebung nachträglich ausführen; public/dist/bundle.js ist STALE (älterer Stand als source) und wird mit rebuild aktualisiert.

## 3. Aktueller Haltepunkt (Last Position)
- **Zuletzt bearbeitete Entität:** `public/js/ui/modal.js` (resetAll() → server-seitige Löschung, Schritt 8); `public/js/config.js` (LEGACY_KEYS entfernt, Schritt 8); `server/routes/PlaybackRoutes.js` + `server/services/playbackService.js` (DELETE `/positions`, Schritt 8)
- **Zustand des Codes/Dokuments:** Alle 10 Schritte 0–8 implementiert. **localStorage vollständig aus public/js/** entfernt (0× Zugriffe; nur Kommentar storage.js:5).** playerCollapsed transient in state. resetAll() löscht am Server (Feeds/Positionen/Downloads). LEGACY_KEYS weg. Server DELETE `/api/playback/positions` ergänzt. Build in dieser Umgebung nicht lauffähig — `npm run build` (esbuild) in produktiver Umgebung nachträglich ausführen; public/dist/bundle.js ist STALE und wird mit rebuild aktualisiert. Schritt 9 (deprecated localStorage-Schlüssel nach Rollout aufräumen) noch OFFEN.

## 4. Sofortige nächste Aktionen (Next Steps)
1. **Priorität 1 (Direkter Einstieg):** Build-Rebuild ist FERTIG (siehe Working Memory). Restliche Validierung (Schritt 10): manuell DevTools Application→LocalStorage nach Boot leer + Cross-Device-Sync + Offline-Audio über SW prüfen.
2. **Priorität 2:** Optional **Schritt 9** — Deprecated-Daten der Alt-Version aufräumen (einmalig): alte localStorage-Schlüssel nach erstem erfolgreichem Server-Load markieren/löschen; optional Umsteiger-Anzeige „Daten wurden vom Server übernommen". Optional/low-value, da alte Keys jetzt tote, harmlose Daten sind (werden nicht mehr gelesen).

## 5. Kritischer Kontext & Variablen (Working Memory)
- **Wichtige Variablen/IDs:** SESSION-Cookie = `podcast_session` (muß `server/config/defaults.js` entsprechen); CACHE-Namen `podany-audio-v1` / `podany-v2` (sw.js unverändert behalten).
- **Klassifizierung (fix):** SERVER = SESSION, FEEDS, POSITIONS, DOWNLOADS. CLIENT-SPEICHER (transient) = CACHED_EPISODES, CACHED_METADATA, THEME, QUEUE.
- **Direkte localStorage-Refs außerhalb storage.js:** `podany_player_collapsed` (playback.js:452,767) → client-only transient; `localStorage.clear()` (ui/modal.js:275 resetAll) → nach Rollout durch server-seitige Löchaufrufe ersetzen.
- **Build-Kommando (FIX):** `npm run build` schlägt in dieser Sandbox fehl, WEIL `node ./node_modules/.bin/esbuild` die ELF-Binary durch die JS-Node-Engine jagt (`SyntaxError: Invalid or unexpected token`). Stattdessen esbuild **direkt** aufrufen: `node_modules/esbuild/bin/esbuild public/js/main.js --bundle --minify --outfile=public/dist/bundle.js --format=esm` (funktioniert, `--version` → 0.23.1). Bundle nach Build frisch verifizierbar an neuen Strings ('Starte Podany','Could not load data from the server','Session Expired') + fehlendem alten 'loadPersistedState'.
- **Bekannte Fallstricke/Fehler:** resetAll() muss nach Rollout Server-Löschungen auslösen (nicht nur Client-State-Reset). THEME/QUEUE resetten nach Reload (transient). Kein `sessionStorage` verwenden (Abschnitt 3 Punkt 1 verbietet es). Node in dieser Umgebung nicht über default `node` aufrufen; ggf. `/home/mk/n/bin/node`.
- **Architekturentscheidungen:** Storage bleibt Abstraktion, damit Manager (sync.js, playback.js, queue.js, etc.) unverändert weiterarbeiten. Cache für Episoden/Metadaten = Arbeitsspeicher (`state.allEpisodes`, `state.feedMetadata`) + SW Cache API für Offline-Audio.
